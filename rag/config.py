"""
全局配置 —— 按模块分类，可选功能均有独立开关。
"""

import os

# ============================================================
# Embedding 模型
# ============================================================
EMBED_MODEL_NAME = "qwen3-embedding:8b"
EMBED_OLLAMA_BASE_URL = "http://10.245.100.186:12434"   # 远程 Ollama 服务地址
EMBED_BATCH_SIZE = 512
EMBED_VECTOR_DIM = 4096            # qwen3-embedding:8b 输出维度（用于 FAISS）

# ============================================================
# HNSW 索引参数（替代 IndexFlatIP，大幅加速检索）
# ============================================================
HNSW_M = 16                      # 每个节点的最大连接数（越大越精确，但构建/检索越慢，推荐 16~64）
HNSW_EF_CONSTRUCTION = 200       # 构建时的搜索宽度（越大越精确，但构建越慢，推荐 100~500）
HNSW_EF_SEARCH = 64              # 检索时的搜索宽度（运行时参数，越大越精确但越慢，推荐 32~128）

# ============================================================
# 最终回答 LLM
# ============================================================
# provider: "ollama"（本地）| "davy"（云端）
ANSWER_PROVIDER = "davy"

# --- Ollama ---
ANSWER_OLLAMA_MODEL = "qwen3.5:9b"
ANSWER_OLLAMA_TEMPERATURE = 0.2
ANSWER_OLLAMA_TIMEOUT = 300.0

# --- Davy 云端连接（answer / rewrite 共用） ---
DAVY_BASE_URL = "https://davy.labs.lenovo.com:5000/v1"
DAVY_MODEL_NAME = "nemotron-3-ultra"        # 当前使用的主模型
DAVY_MODEL_NAME_GPT_OSS = "gpt-oss-120b"       # 备选模型（原主模型，保留）
DAVY_CERT_PATH = os.path.join(os.path.dirname(__file__), "..", "assets", "sha2rootca-ca_subca1.cer")
DAVY_API_KEY = "ZmU0Nt7kbSGH8SEIwvFCSVYwGDiTSQNMezbfLZW_3Aw"
DAVY_TIMEOUT = 120.0
DAVY_TEMPERATURE = 0.2

# ============================================================
# 查询重写（三路策略：NL改写 + HyDE + BM25关键词扩展）
# ============================================================
REWRITE_ENABLED = True           # 开关
# provider: "ollama" | "davy"
REWRITE_PROVIDER = "davy"

# --- Ollama ---
REWRITE_OLLAMA_MODEL = "qwen3.5:9b"
REWRITE_OLLAMA_TEMPERATURE = 0.1
REWRITE_OLLAMA_TIMEOUT = 180.0

# 三路改写共用的原著背景知识，确保 LLM 在正确的语境下工作
_NOVEL_CONTEXT = (
    '【原著背景 —— 你必须基于此背景进行改写】\n'
    '《遥远的救世主》是豆豆创作的一部长篇现实主义小说，故事发生在1990年代至2000年代初的中国。\n'
    '这是一部严肃的商战/爱情/哲学题材小说，不是科幻、不是谍战、不是武侠、不是奇幻。\n\n'
    '核心人物：\n'
    '- 丁元英：男，1959年生于成都，北京户口。清华大学毕业，柏林洪堡大学经济学硕士。\n'
    '  先后就职于柏林H.N.S国际金融投资公司、北京通达证券公司、柏林《世界经济周刊》\n'
    '  （任经济发展战略研究员），后创办个人私募基金。私募基金解散后隐居古城，\n'
    '  通过"杀富济贫"式的商业策划帮助王庙村脱贫，创办格律诗音响公司。\n'
    '  他是一个商界怪才，对文化属性有深刻洞察，被书中人物称为"魔"或"极品混混"。\n'
    '- 芮小丹：女，古城公安局刑警，后与丁元英相恋。\n'
    '- 韩楚风：丁元英的好友，正天集团总裁。\n'
    '- 肖亚文：丁元英的前助理，后接管格律诗公司。\n'
    '- 欧阳雪：芮小丹的好友，饭店老板。\n'
    '- 叶晓明、冯世杰、刘冰：王庙村音响创业者。\n\n'
    '原著风格：白描叙事，大量对话推动情节，语言简洁冷峻，涉及商业逻辑、文化属性、\n'
    '佛法禅意、音响技术等话题。时代背景为1990年代末至2000年代初，使用当时的科技\n'
    '和生活方式（如座机电话、传真机、音响、CD唱片等）。\n'
)

# 路径 A：自然语言改写 —— 贴近原著措辞，用于向量检索
REWRITE_NL_PROMPT = (
    '你是一个查询重写助手，为《遥远的救世主》知识库的检索系统服务。\n'
    '{novel_context}\n'
    '用户会用口语化的问题提问小说相关的内容，你需要将用户的问题改写成更贴近\n'
    '原著措辞的表述，使用原著中可能出现的句式、词汇和称谓。\n\n'
    '改写策略：\n'
    '- 称谓：用原著中人物的正式称谓（如"丁元英""芮小丹""韩楚风"等全名，\n'
    '  或"肖亚文的前老板""私募基金操盘手""格律诗的幕后推手"等身份描述）\n'
    '- 事件：用原著中描述事件的方式（如"xxx就职于xxx""xxx与xxx发生冲突"\n'
    '  "xxx对xxx的评价""xxx在xxx事件中的决策"）\n'
    '- 措辞：尽量使用原著中可能出现的词汇和句式，避免过于口语化或网络化的表达\n\n'
    '要求：\n'
    '1. 保留原问题的核心语义，不得改变用户想问的事实\n'
    '2. 输出一句自然语言问句或陈述句，简洁明了\n'
    '3. 仅输出改写后的文本，不要添加任何解释或编号\n\n'
    '用户问题：{{query}}\n'
    '改写结果：'
).format(novel_context=_NOVEL_CONTEXT)

# 路径 B：HyDE（假设性文档嵌入）—— 生成假设性回答段落，用于向量检索
REWRITE_HYDE_PROMPT = (
    '你是一个熟知《遥远的救世主》原著的专家。\n'
    '{novel_context}\n'
    '请根据你对原著内容的了解，用原著中可能出现的文字风格，写一段假设性的回答\n'
    '来回应以下问题。\n\n'
    '写作要求：\n'
    '1. 严格基于上述原著背景，在1990年代-2000年代初中国现实主义语境下写作\n'
    '2. 仿照原著的文字风格：白描、简洁、冷峻，使用第三人称叙述\n'
    '3. 如果问题涉及丁元英，请基于他的真实履历（金融投资→证券→经济研究→私募基金→扶贫创业）来写\n'
    '4. 控制在200字以内\n'
    '5. 仅输出假设性回答文本，不要添加任何解释\n\n'
    '用户问题：{{query}}\n'
    '假设性回答：'
).format(novel_context=_NOVEL_CONTEXT)

# 路径 C：BM25 关键词扩展 —— 展开为关键词列表，用于 BM25 检索
REWRITE_KW_PROMPT = (
    '你是一个为《遥远的救世主》原著设计检索关键词的专家。\n'
    '{novel_context}\n'
    '将以下问题改写成适合在原著中进行BM25关键词检索的形式。\n'
    '列出原文中可能出现的所有相关关键词、短语、同义词、近义词，用空格分隔。\n\n'
    '关键词选取策略：\n'
    '1. 实体名称：问题涉及的所有人物全名、简称、身份、关系描述\n'
    '   例：丁元英 元英 丁哥 私募基金操盘手 格律诗幕后推手\n'
    '2. 近义扩展：对语义概念补充同义/近义表达，每个概念只扩展一次，不要组合\n'
    '   例："工作"→ 就职 任职 职业 简历 履历 工作经历\n'
    '   ❌ 禁止：不要给每个动作和每个对象做全排列组合（如"给丁元英打电话 给丁哥打电话..."）\n'
    '3. 原著术语：原著中可能出现的相关专业术语\n'
    '   例：私募基金 音响 格律诗 王庙村 扶贫 文化属性\n'
    '4. 控制总关键词数在 20~40 个，不要过多，去掉冗余重复\n'
    '5. 关键词按重要性从高到低排列，最重要的排最前面\n'
    '6. 仅输出关键词串，用空格分隔，不要添加任何解释\n\n'
    '用户问题：{{query}}\n'
    '关键词：'
).format(novel_context=_NOVEL_CONTEXT)

# ============================================================
# 检索
# ============================================================
RETRIEVAL_TOP_K = 30             # 每路检索器原始召回量（向量 / BM25 共用）
RERANK_CANDIDATE_POOL_SIZE = 20  # RRF 融合后送入 reranker 的候选数
FINAL_TOP_K = 10                 # 最终返回量（rerank 后 or 无 reranker 时的最终 top-k）

# ============================================================
# RRF 融合
# ============================================================
RRF_K = 60.0                     # RRF 平滑参数

# ============================================================
# 摘要冗余过滤
# ============================================================
# 若摘要节点覆盖的原始 chunk 被召回比例 ≥ 此值，说明已被原文覆盖，删除该摘要节点
SUMMARY_REDUNDANCY_THRESHOLD = 0.5

# ============================================================
# 多路检索优化 —— 相邻分数比过滤（gap detection）
# ============================================================
GAP_THRESHOLD = 0.2              # 相邻块分数下降比例阈值
MIN_CANDIDATES = 10              # 每路过滤后最小候选块数量下限（防止过早截断导致块过少）
MAX_CANDIDATES = 30              # 每路过滤后最大候选块数量上限

# 每路最小分数阈值（分数量纲不同，各自独立）
VECTOR_MIN_SCORE = 0.1            # 向量检索（cosine similarity，约 P25 位置）
BM25_MIN_SCORE = 0.3              # BM25 检索（有效分数约 0.45~0.79，取 ~60% 为底线）

# ============================================================
# 查询分解（将复杂查询拆分为子查询，分别检索后合并）
# ============================================================
DECOMPOSE_ENABLED = True          # 开关
DECOMPOSE_MAX_SUB_QUERIES = 5     # 最大子查询数量
DECOMPOSE_LLM_TEMPERATURE = 0.1   # 分解/分类 LLM 温度

# 复杂度判断 prompt（轻量，只回答是/否）
DECOMPOSE_CLASSIFY_PROMPT = (
    '判断以下查询是否属于"复杂查询"（需要拆分为多个子问题才能完整回答）。\n\n'
    '复杂查询的特征：\n'
    '- 包含多个独立问题（用"和"、"以及"、"还有"连接）\n'
    '- 需要对比或比较（"区别"、"异同"、"对比"）\n'
    '- 包含因果关系（"为什么...导致..."、"...的影响"）\n'
    '- 包含时间顺序（"先...后..."、"然后"、"之后"）\n\n'
    '请只回答"是"或"否"。\n\n'
    '查询：{query}'
)

# 子查询拆解 prompt
DECOMPOSE_PROMPT = (
    '将以下复杂查询拆分为多个独立的子问题，每个子问题可以单独检索回答。\n\n'
    '要求：\n'
    '1. 每个子问题完整、独立、可单独检索\n'
    '2. 保持子问题的检索语义清晰，使用原著中可能出现的措辞\n'
    '3. 最多拆分为 {max_sub} 个子问题\n'
    '4. 每行一个子问题，用数字编号（如"1. xxx"）\n'
    '5. 不要添加任何解释性文字\n\n'
    '查询：{query}\n\n'
    '子问题：'
)

# ============================================================
# 重排序（bge-reranker-v2-m3：通过 vLLM /v1/rerank 端点调用）
# ============================================================
RERANK_ENABLED = True            # 开关
RERANK_BASE_URL = "http://10.245.100.186:12345"
RERANK_MODEL_NAME = "bge_reranker_v2_m3"
RERANK_TIMEOUT = 30.0
RERANK_TEXT_MAX_LENGTH = 8192     # 送入 reranker 的文本最大长度（bge-reranker-v2-m3 最大 8192 tokens）
RERANK_MAX_CONCURRENCY = 4       # ThreadPoolExecutor 最大并发数（vLLM continuous batching 自动合并）

# ============================================================
# 分块
# ============================================================
CHUNK_SIZE = 1024
CHUNK_OVERLAP = 102                 # 单侧 overlap 上限（字符数），chunk_size 的 10%，相邻 chunk 重叠约此值

# ============================================================
# 摘要树
# ============================================================
SUMMARY_TREE_ENABLED = True       # 开关：启用层次化摘要树
SUMMARY_LLM_PROVIDER = "davy"     # 生成摘要使用的 LLM："ollama" | "davy"
SUMMARY_GROUP_SIZE = 10           # L2→L3 每组合并的章节数
SUMMARY_PARENT_RATIO = 0.20       # L2 小节摘要字数 = 输入文本总字数 × 此比例
SUMMARY_CHAPTER_RATIO = 0.15      # L3 章节摘要字数 = 输入文本总字数 × 此比例
SUMMARY_BOOK_CHARS = 500          # L4 全书摘要字数上限
SUMMARY_BATCH_SIZE = 20          # 每批次生成叶子摘要的数量（用于进度日志）
SUMMARY_MAX_CONCURRENCY = 2      # 摘要生成的最大并发请求数（经压测，>2 会触发 429）
SUMMARY_LEAF_BATCH_SIZE = 5      # 每次 LLM 调用合并的 chunk 数（减少 API 调用次数，约 2x 加速）
SUMMARY_LLM_TEMPERATURE = 0.1

# 叶子摘要 prompt —— 单 chunk 模式（兼容保留）
SUMMARY_LEAF_PROMPT = (
    '你是一个文档摘要助手。请用一句话概括以下文档片段的主题和核心内容。\n'
    '要求：\n'
    '1. 控制在 100 字以内\n'
    '2. 保留关键实体和术语\n'
    '3. 使用陈述句，不包含编号或格式标记\n'
    '4. 仅输出摘要文本，不要添加任何解释\n\n'
    '文档片段：\n{chunk_text}\n\n'
    '一句话摘要：'
)

# 叶子摘要 prompt —— 批量模式（一次请求处理多个 chunk）
SUMMARY_LEAF_BATCH_PROMPT = (
    '你是一个文学作品的精读摘要助手。请为以下《遥远的救世主》的多个片段分别生成一句话摘要。\n\n'
    '【摘要质量要求】\n'
    '1. 每条摘要必须包含「谁 + 做了什么 + 为什么重要」三个要素，缺一不可\n'
    '2. 优先保留以下三类信息（按优先级排序）：\n'
    '   a) 人物关系的确立或变化（如：谁通过什么事认识了谁、谁对谁产生了什么看法）\n'
    '   b) 情节推进的关键决策或转折（如：某人决定做某事、某事件导致的结果）\n'
    '   c) 体现人物性格或价值观的言行（如：某人的关键判断、重要评价）\n'
    '3. 人物名、地名、组织名、时间节点必须原样保留，不得替换或省略\n'
    '4. 每条摘要控制在 100 字以内；宁可省略次要细节，不可丢失核心信息\n'
    '5. 使用简洁的陈述句，不包含编号或格式标记\n\n'
    '【禁止行为】\n'
    '- 禁止编造原文中没有的内容\n'
    '- 禁止用"某个人""某个地方"等模糊代词替代具体名称\n'
    '- 禁止添加任何解释或评价性文字\n\n'
    '输出格式：\n'
    '[1] 第一条摘要\n'
    '[2] 第二条摘要\n'
    '...\n\n'
    '文档片段：\n{chunks}\n\n'
    '摘要列表：'
)

# L1→L2 父级摘要 prompt：直接从章节原文生成章节摘要（不经过叶子摘要合并）
SUMMARY_PARENT_FROM_RAW_PROMPT = (
    '你是一个文学作品的章节摘要专家。下面是一段《遥远的救世主》的原文，请生成该章节的高质量摘要。\n\n'
    '【摘要策略】\n'
    '你的目标是：让一个没读过原文的人，看完摘要后能准确回答「这一章讲了什么」和「为什么重要」。\n\n'
    '【取舍原则 —— 你必须严格遵循】\n'
    '✅ 保留：\n'
    '- 推动情节发展的关键事件（谁做了什么、导致了什么）\n'
    '- 人物关系的变化（新关系的建立、旧关系的转折、情感态度的变化）\n'
    '- 体现核心主题的对话或观点（如：关于文化属性、救世主、商业逻辑的讨论）\n'
    '- 关键数字、金额、时间、地点\n\n'
    '❌ 删除：\n'
    '- 环境描写、外貌描写、心理描写（除非直接服务于情节）\n'
    '- 过渡性对话（寒暄、客套、无关紧要的闲聊）\n'
    '- 动作细节（如"她理了理头发""他喝了一口咖啡"）\n'
    '- 重复性信息\n\n'
    '【结构要求】\n'
    '1. 按事件发生的时间顺序组织，每个事件用 1-2 句话概括\n'
    '2. 事件之间用逻辑连接词串联（因此、随后、与此同时、然而等），体现因果链\n'
    '3. 控制在 {max_chars} 字以内；如果原文过长，优先保留前 3-5 个最重要的事件\n'
    '4. 使用连贯的陈述句，不包含编号或格式标记\n\n'
    '【禁止行为】\n'
    '- 禁止不分主次地罗列所有对话内容\n'
    '- 禁止编造原文中没有的因果关系\n'
    '- 禁止添加"本章写了""这段话说明"等元描述\n\n'
    '章节：{section_name}\n'
    '原文：\n{chapter_text}\n\n'
    '章节摘要：'
)

# L2+ 父级摘要 prompt：合并子摘要（L3=L2合并, L4=L3合并）
SUMMARY_PARENT_PROMPT = (
    '你是一个文学作品的宏观摘要专家。下面提供了多个章节的摘要，请用一段话整合为更高层次的摘要。\n\n'
    '【整合策略】\n'
    '你的任务不是简单拼接，而是做三件事：\n'
    '1. 识别跨章节的主题线索（如：丁元英的商业布局、芮小丹的情感变化、格律诗的诉讼进程）\n'
    '2. 找出章节之间的因果链（前一章的事件如何导致后一章的后果）\n'
    '3. 提炼出最重要的 3-5 个事件节点，组成一个连贯的叙事弧线\n\n'
    '【质量标准】\n'
    '- 每个事件节点用 1-2 句话，包含事件本身 + 其产生的后果\n'
    '- 不同主题线索之间用"与此同时""另一方面"等分隔\n'
    '- 必须有明确的因果关系，而非简单的"然后...然后..."\n'
    '- 控制在 {max_chars} 字以内\n\n'
    '【禁止行为】\n'
    '- 禁止逐章罗列，必须提炼跨章节的主题\n'
    '- 禁止遗漏关键人物的最终结局\n'
    '- 禁止添加"这些章节""综上所述"等元描述\n\n'
    '各章节摘要：\n{child_summaries}\n\n'
    '概括摘要：'
)

# ============================================================
# 术语映射（通俗名 → 原文术语）
# ============================================================
TERM_MAP_PATH = os.path.join(os.path.dirname(__file__), "..", "assets", "terminology.json")

# ============================================================
# 持久化路径
# ============================================================
PERSIST_DIR = os.path.join(os.path.dirname(__file__), "..", "data", "vector")
FAISS_PERSIST_DIR = os.path.join(os.path.dirname(__file__), "..", "data", "vector", "faiss")
BM25_DIR = os.path.join(os.path.dirname(__file__), "..", "data", "bm25")
DATA_DIR = os.path.join(os.path.dirname(__file__), "..", "data", "raw")
SUMMARY_TREE_DIR = os.path.join(os.path.dirname(__file__), "..", "data", "summary_tree")
CHUNKS_DIR = os.path.join(os.path.dirname(__file__), "..", "data", "chunks")
GRAPH_DB_DIR = os.path.join(os.path.dirname(__file__), "..", "data", "graph_db")

# ============================================================
# 知识图谱（PropertyGraph + Kuzu）
# ============================================================
GRAPH_ENABLED = True               # 开关：是否启用知识图谱构建与检索

# 图专用分块参数（比检索分块更大，给 LLM 更完整的上下文理解人物关系）
GRAPH_CHUNK_SIZE = 1500            # 图抽取的 chunk 大小（字符数）
GRAPH_CHUNK_OVERLAP = 200          # 图抽取的 chunk 重叠

# 图检索设置
GRAPH_RETRIEVAL_MAX_TRIPLES = 20   # 每次图检索最多返回的三元组数量
GRAPH_RETRIEVAL_DEPTH = 2          # 图遍历深度（从匹配实体出发的跳数，1=直接邻居，2=邻居的邻居）
GRAPH_RETRIEVAL_TOP_K = 5          # 匹配实体时取 top-k 个实体作为入口

# 实体/关系抽取 LLM 配置
GRAPH_EXTRACT_LLM_PROVIDER = "davy"   # "ollama" | "davy"
GRAPH_EXTRACT_MAX_CONCURRENCY = 2     # 抽取并发数
GRAPH_EXTRACT_BATCH_SIZE = 5          # 每批处理的 chunk 数

# 三元组校验 LLM 配置（不同模型交叉校验更可靠）
GRAPH_VALIDATE_LLM_PROVIDER = "davy"    # "ollama" | "davy"
GRAPH_VALIDATE_DAVY_MODEL = "gpt-oss-120b"  # 校验用 Davy 模型（与原主模型不同，交叉校验）
GRAPH_VALIDATE_LLM_MODEL = "qwen3.6:27b"  # 校验用 ollama 模型（仅 provider=ollama 时生效）
GRAPH_VALIDATE_LLM_BASE_URL = "http://10.245.100.186:12434"
GRAPH_VALIDATE_LLM_TIMEOUT = 3600.0  # 1 小时，基本等于无限等待
GRAPH_VALIDATE_LLM_TEMPERATURE = 0.1

# 是否启用 LLM 二次校验
GRAPH_VALIDATE_ENABLED = True  # 开启后每个 chunk 做完规则过滤再送 LLM 校验

# 校验策略：置信度阈值（低于此阈值的关系才送 LLM 校验，设为 2.0 表示全部校验）
GRAPH_VALIDATE_CONFIDENCE_THRESHOLD = 2.0

# Schema 自动成长阈值（未知类型出现次数达到此值后自动升级为 learned 类型）
GRAPH_SCHEMA_GROWTH_THRESHOLD = 5

# 是否启用实体名称规范化（别名 → Canonical Name）
GRAPH_CANONICALIZE_ENABLED = True

# 实体/关系抽取 Prompt（开放 Schema，质量优先，不限制数量）
GRAPH_EXTRACT_PROMPT = (
    '你是一个文学作品知识图谱构建专家。请从以下《遥远的救世主》原文片段中抽取实体和关系。\n\n'
    '【核心原则：宁可多抽，不可漏抽】\n'
    '后续会有独立的校验环节来过滤错误关系，你的任务是尽可能不遗漏任何可能有价值的关系。\n'
    '即使不确定某条关系是否准确，也请抽取出来——校验环节会帮你判断。\n\n'
    '【实体抽取规则】\n'
    '只抽取「具名实体」：有具体名称的人物、组织、地点、物品、概念、事件、称号等。\n'
    '类型标签由你根据上下文自行判断，如：人物、组织、地点、事件、物品、概念、称号、金钱数额……\n'
    '为每个实体生成一段简短描述（20~80字），概括身份、角色、关键特征，必须基于原文。\n\n'
    '以下内容【禁止】作为实体抽取：\n'
    '× 人称代词：我、你、他、她、它、我们、你们、他们、她们、吾、汝、其、之\n'
    '× 泛指称谓：某人、一个人、那个女人、这位先生、大家、所有人\n'
    '× 时间副词：今天、昨天、刚才、后来、那时、这时\n'
    '× 普通生活物品：桌子、椅子、门、窗户（除非在情节中有特殊意义）\n'
    '× 单字名称（极易是代词或噪声）\n'
    '如果原文片段中没有任何具名实体，请返回空列表，不要强行抽取。\n\n'
    '【关系抽取规则 —— 召回优先】\n'
    '关系谓词必须简洁（2-6个字），如：创办、任职于、爱慕、赠送、评价、冲突、合作、属于、借钱、委托……\n'
    '为每个关系生成一段简短描述（20~80字），说明关系的背景和上下文，必须基于原文。\n'
    '以下关系【禁止】抽取：\n'
    '× 场景级日常动作：打电话、前往、递交、支付、交付、握手、打招呼、送别、播放、拨通、递给、接过、倒了（水/茶/酒）\n'
    '× 无因果的共处关系：A和B在一起、A旁边有B\n\n'
    '【质量要求】\n'
    '1. 关系谓词尽量用"A 创办 B"而非"A 创办了 B"，去掉"了"等虚词\n'
    '2. 注意：归属权、性别合理性等细节问题由后续校验处理，你只需尽力抽取，不要因顾虑准确性而漏抽\n'
    '3. description 必须基于原文，不要编造原文没有的背景信息\n\n'
    '输出格式（严格 JSON，不要输出其他内容）：\n'
    '{{\n'
    '  "entities": [\n'
    '    {{"name": "实体名", "type": "类型标签", "description": "基于原文的简短描述"}}\n'
    '  ],\n'
    '  "relations": [\n'
    '    {{"subject": "主体", "predicate": "关系", "object": "客体", "description": "关系的背景和上下文"}}\n'
    '  ]\n'
    '}}\n\n'
    '原文片段：\n{chunk_text}\n\n'
    '抽取结果（JSON）：'
)

# 三元组校验 Prompt（判别式，不同模型交叉校验）
GRAPH_VALIDATE_PROMPT = (
    '你是知识图谱质检员。请根据以下原文，逐条检查三元组是否与原文事实一致。\n\n'
    '【原文】\n{chunk_text}\n\n'
    '【待校验的三元组】\n{triples_text}\n\n'
    '【校验规则】\n'
    '1. 关系是否确实存在于原文中？主语和宾语的关系是否准确？\n'
    '2. description 是否基于原文、没有编造原文没有的信息？如有编造请修正\n'
    '3. 特别注意归属权：某人只是"看/打开/翻看/递给/归还"某物，不等于持有/拥有/获得该物\n'
    '4. 注意性别：包含"女子组"、"女"的物品/证书只能属于女性角色，不能归到男性角色名下\n'
    '5. 注意身份：警察证件只能属于警察，法律证件只能属于律师\n'
    '6. 注意谓词是否合理：如果原文只说"A打开B的档案夹"，不能说A持有档案夹里的东西\n\n'
    '【输出格式 - 严格JSON，不要输出其他内容】\n'
    '{{\n'
    '  "valid":[0,2],\n'
    '  "invalid":[1,3],\n'
    '  "corrected":[{{"index":1,"subject":"芮小丹","predicate":"持有","object":"警官大学毕业证","description":"修正后的描述"}}],\n'
    '  "reasons":{{"1":"原文中证书属于芮小丹，丁元英只是翻看"}}\n'
    '}}\n'
    '其中序号从0开始，对应待校验三元组的顺序。\n'
    '如果三元组的关系错误但可以修正（如主语/宾语/谓词写反了，或description需要修正），请在 corrected 中给出修正版，\n'
    '修正版必须包含 index（原序号）、subject、predicate、object、description 五个字段。\n'
    '无法修正的（如纯属虚构、不存在的关系）直接放入 invalid。\n\n'
    '校验结果（JSON）：'
)

# ============================================================
# Prompt 模板
# ============================================================
QA_TEMPLATE_STR = (
    '你是一个严谨的客服助手，必须严格依据下面提供的"参考资料"回答问题。\n'
    '如果资料中没有答案，请直接说"根据已有资料无法回答"。\n'
    '回答时请尽量引用原文，并注明文件名称。\n\n'
    '参考资料：\n{context_str}\n\n'
    '用户问题：{query_str}\n'
    '回答：'
)

# ============================================================
# 调试
# ============================================================
DEBUG = True                      # 总调试开关：控制是否输出各步骤的 top-3/top-5 详情